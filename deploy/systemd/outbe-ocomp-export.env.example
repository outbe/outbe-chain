# Snapshot-exporter-only read credentials for the Mongo projection transport.
# Exported bodies are independently closed against the finalized CE root.
OUTBE_PROJECTION_MONGODB_URI=<mongodb://projection-reader@host/outbe>
OUTBE_PROJECTION_MONGODB_DATABASE=<outbe-projection-database>
